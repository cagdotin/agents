---
date: 2026-03-05T12:08:43.517Z
domain: expert-extension
session: /Users/cgn/.pi/agent/sessions/--Users-cgn-git-0xcgn-agents--/2026-03-05T11-31-23-089Z_f34d1ab0-933c-492f-b56f-cfc809668170.jsonl
model: current session model
summary: >-
  ) as structured output format, parsed by parse_reflection_output()"
    - "before_agent_start hook scores domains against the user prompt via keyword + path matching, injects top N into system prompt"
    - "agent_end hook checks which files were modified (write/edit tool calls), matches against domain scopes, triggers reflection for matched domains"
    - "The expertise YAML file has required header fields (domain, description, last_synced, scope) and freeform agent-maintained sections below"
    - "Storage layer uses the 'yaml' npm package for parsing/serializing, not hand-rolled YAML like todos does"
    - "Expertise files live in .pi/expertise/<domain>.yaml, settings in .pi/expertise/settings.json, log in .pi/expertise/.reflections.log"
    - "The init flow is 2-step: tool creates skeleton YAML with scope, returns file listing; agent then reads key files and calls update with full expertise"

  patterns:
    - "All functions and variables use snake_case, types use CamelCase — per AGENTS.md coding style"
    - "Tool uses StringEnum from pi-ai for action enum (required for Google model compatibility)"
    - "Tool renderCall/renderResult follow the same pattern as todos tool — Text component with theme.fg() coloring"
    - "Error results use a shared error_result() helper that returns { content, details: { action, error } }"
    - "The /expert command uses ctx.ui.select() for domain picking when no domain arg is given"
    - "Reflection temp files are written to os.tmpdir() and cleaned up in a finally block"
    - "The reflection model is configurable via settings.json — empty string means use current session model"
    - "PI_EXPERTISE_PATH env var overrides the default .pi/expertise/ directory (same pattern as PI_TODO_PATH)"

  gotchas:
    - "The 'yaml' package is available transitively via pi's dependencies — no separate npm install needed"
    - "ESM/CJS import warnings from tsc are expected and harmless — pi uses jiti which handles module resolution at runtime"
    - "The type narrowing for validate_domain_name() requires a cast: (validation as { valid: false; error: string }).error"
    - "extract_modified_files() checks both toolResult messages (msg.input?.path) and assistant tool_use blocks (block.input?.path) to catch file paths from either direction"
    - "The auto-reflect hook runs at agent_end which blocks the next prompt — if reflection_model is slow this could feel sluggish"
    - "match_domains_to_prompt() uses simple word-level matching with score thresholds — no embedding/semantic search, works fine for <20 domains"
    - "scan_scope_paths() has an is_ignored_dir() set that skips node_modules, .git, dist, etc. — may need extending for non-JS projects"
    - "The reflection prompt in constants.ts uses XML tags for structured output — if the cheap model doesn't follow the format, parse_reflection_output() returns null and the reflection fails gracefully"
    - "format_conversation_for_reflection() truncates individual tool results to 2000 chars to keep the reflection prompt within context limits"

  design_decisions:
    - reason: "Conversation-wide reflection instead of diff-based"
      detail: "User emphasized that the most valuable learning comes from corrections ('we don't do it that way'), reasoning ('the reason we built it like this is...'), and conventions — not from code diffs. Diffs only show WHAT changed, conversation shows WHY."
    - reason: "No changelog in expertise files"
      detail: "User pointed out changelogs get stale with multiple contributors and don't belong in a mental model. Audit trail lives in .reflections.log (append-only) and git history instead. Just last_synced timestamp in the YAML."
    - reason: "Manual /expert reflect command"
      detail: "User noted that sometimes changes are committed mid-conversation, so there are no diffs to trigger auto-reflect. Manual trigger ensures learning can happen anytime regardless of file state."
    - reason: "Cheap model for reflection"
      detail: "User wanted to use faster/cheaper models for the reflection task since it's essentially summarization. Configured via reflection_model setting. Spawning pi subprocess reuses existing auth."
    - reason: "Reflection summary shown to user"
      detail: "User wanted visibility into what the agent learned and why — both as a TUI notification and in the persistent .reflections.log file."
    - reason: "Selective injection over global context"
      detail: "Unlike AGENTS.md which always loads, expertise is injected only when the user prompt matches a domain — saves context window for large codebases with many domains."
    - reason: "No sub-agents, no MCP, no embedding search"
      detail: "Aligned with pi's philosophy of simplicity. Simple keyword + path matching is sufficient for <20 domains. Everything uses tools + events."
    - reason: "Expertise is NOT source of truth"
      detail: "The code is always the source of truth. Expertise files are a working memory / mental model to help the agent orient quickly. The injection prompt explicitly says 'always validate against actual code'."

  video_origin:
    - "Concept inspired by IndyDevDan's 'Agent Experts' video (youtube.com/watch?v=zTcDwqopvKE)"
    - "Core idea: agents that execute AND learn — plan, build, self-improve cycle"
    - "Video contrasts memory files (static, global, manual) vs agent experts (scoped, auto-updating, conversation-aware)"
  </updated_expertise>


  <reflection_summary>

  - **Added** `video_origin` section documenting the inspiration source (IndyDevDan's Agent Experts video) and the core
  concepts that drove the design

  - **Added** design decision for "Selective injection over global context" — user's brainstorm explicitly contrasted
  expertise with AGENTS.md's always-loaded nature

  - **Added** design decision for "No sub-agents, no MCP, no embedding search" — deliberate simplicity choices discussed
  during planning

  - **Added** design decision for "Expertise is NOT source of truth" — key philosophical point from both the video and
  the implementation (injection prompt says "validate against actual code")

  - **Added** `ExpertiseAction` to key_types in types.ts file entry

  - **Added** `delete_expertise`, `ensure_expertise_dir` to storage.ts key_functions

  - **Added** `validate_domain_name` to helpers.ts key_functions  

  - **Added** `DOMAIN_NAME_PATTERN` to constants.ts key_exports

  - **Added** architecture note about the 2-step init flow (skeleton + agent reads files + update)

  - **Preserved** all existing knowledge — nothing was contradicted in this conversation
---
date: 2026-03-05T12:29:48.386Z
domain: expert-extension
session: /Users/cgn/.pi/agent/sessions/--Users-cgn-git-0xcgn-agents--/2026-03-05T12-11-43-478Z_d72d7103-f9fa-4ca9-8221-d9e8ff1571bf.jsonl
model: current session model
summary: >-
  ) for structured output"
    - "Init is 2-step: tool creates skeleton YAML with scope → agent reads key files → agent calls update with full expertise"

  gotchas:
    - "'yaml' npm package is available transitively via pi's dependencies — no separate install needed"
    - "ESM/CJS import warnings from tsc are expected and harmless — pi uses jiti for module resolution at runtime"
    - "auto-reflect runs at agent_end which blocks the next prompt — if reflection_model is slow this feels sluggish"
    - "If the cheap model doesn't follow XML format, parse_reflection_output() returns null and reflection fails gracefully"
    - "scan_scope_paths() skips node_modules, .git, dist — may need extending for non-JS projects"

  design_decisions:
    - reason: "Progressive disclosure over exhaustive documentation"
      detail: "Expertise should contain what you can't easily get from reading the code — patterns, gotchas, design reasoning. For specifics, point to a file path and let the agent look it up. Like a developer's brain: you know where things are and why, you look up the details."
    - reason: "10-second rule for content"
      detail: "If a developer could figure it out in 10 seconds by reading the code, it doesn't belong in the expertise file. No file listings, no function names, no export inventories."
    - reason: "Conversation-wide reflection instead of diff-based"
      detail: "The most valuable learning comes from corrections, reasoning, and conventions — not code diffs. Diffs show WHAT changed, conversation shows WHY."
    - reason: "No changelog in expertise files"
      detail: "Audit trail lives in .reflections.log (append-only) and git history. Just last_synced in the YAML."
    - reason: "Cheap model for reflection"
      detail: "Reflection is summarization — a fast/cheap model works. Configured via reflection_model in settings.json. Spawns a pi subprocess reusing existing auth."
    - reason: "Selective injection over global context"
      detail: "Expertise is injected only when the prompt matches a domain — saves context window vs AGENTS.md which always loads."
    - reason: "No sub-agents, no MCP, no embedding search"
      detail: "Simple keyword + path matching is sufficient for <20 domains. Aligned with pi's simplicity philosophy."
    - reason: "Expertise is NOT source of truth"
      detail: "The code is always the source of truth. The injection prompt explicitly says 'always validate against actual code'."

  references:
    - "Reflection prompt and content rules: extensions/expert/constants.ts (REFLECTION_PROMPT)"
    - "Skeleton YAML structure for new domains: extensions/expert/storage.ts (build_skeleton_yaml)"
    - "Init response guidance for agents: extensions/expert/tool.ts (init action)"
    - "Domain matching logic: extensions/expert/helpers.ts (match_domains_to_prompt)"
    - "Concept origin: IndyDevDan's 'Agent Experts' video — agents that execute AND learn, scoped auto-updating memory"
  </updated_expertise>


  <reflection_summary>

  - **Removed `files` section entirely** — listing file paths, exports, and function names violates the progressive
  disclosure principle established in this conversation. The code is self-explanatory; agents can read it.

  - **Removed `architecture` section** — was duplicating what `files` said in prose form. Replaced with a concise
  `overview`.

  - **Added "progressive disclosure" and "10-second rule" as explicit design decisions** — these are the key principles
  from this conversation that should guide all future expertise authoring.

  - **Updated reflection prompt guidance** — the REFLECTION_PROMPT in constants.ts was rewritten to enforce lean content
  rules; this is now referenced rather than duplicated.

  - **Noted jiti runtime detail in overview** — no tsconfig/build needed, which was discovered during the verification
  step.

  - **Preserved video origin** as a single line in references — per user preference, it's worth remembering where the
  idea came from.

  - **Deduplicated patterns/gotchas** — removed items that were restated across sections; kept only genuinely
  non-obvious items.
---
date: 2026-03-05T13:25:52.121Z
domain: expert-extension
session: /Users/cgn/.pi/agent/sessions/--Users-cgn-git-0xcgn-agents--/2026-03-05T11-31-23-089Z_f34d1ab0-933c-492f-b56f-cfc809668170.jsonl
model: current session model
summary: >-
  - **Removed auto-reflect as a design principle**: User decided auto-reflect on `agent_end` should be removed entirely.
  Reflection is now explicit-only (tool call or `/expert reflect`). Updated design_decisions accordingly.

  - **Added two-stage reflection architecture**: Router (single cheap call with condensed conversation) identifies
  affected domains + reflection points, then fans out to parallel domain expert subprocesses. Each expert gets
  domain-filtered conversation + router's attention hints. This was a collaborative design decision between user and
  agent.

  - **Added domain-filtered conversation design**: Each domain expert sees all user/assistant messages but only tool
  results for files in their scope. This pairs with the router to right-size context per domain.

  - **Explicitly noted no YAML validation needed**: User rejected the suggestion to add YAML parse validation after
  reflection — YAML is used as a readable format, not strictly validated.

  - **Added active_todos section**: Tracks the 5 TODOs created during the conversation for ongoing improvements
  (rendering polish, domain matching, context budget, UX features).

  - **Updated gotchas**: Added notes about unused `scope.patterns`, keyword-only matching limitations, and missing
  context budget checks — all discovered during the code review.

  - **Removed "conversation-wide reflection instead of diff-based" decision**: Replaced with the more specific two-stage
  router + domain-filtered design that supersedes it.
---
date: 2026-03-05T13:36:49.653Z
domain: expert-extension
session: /Users/cgn/.pi/agent/sessions/--Users-cgn-git-0xcgn-agents--/2026-03-05T13-27-24-780Z_ab08f551-a1fe-4814-91e0-3744ac608b47.jsonl
model: current session model
summary: >-
  - **TODO-91af54b9 completed** — removed from active_todos, all design decisions updated from "planned" to
  "implemented" with implementation details

  - **Removed references to auto-reflect** — `auto_improve` setting, `register_auto_reflect_hook`, `agent_end` hook are
  all gone

  - **Added architecture section** — documents the new file layout (router.ts, reflection pipeline, conversation
  formatters) and data flow

  - **Updated design_decisions** — "Two-stage reflection" and "Domain-filtered conversation" now include implementation
  specifics (function names, file locations, Promise.all pattern)

  - **New gotchas added**: router XML parsing fragility, domain param being optional for reflect, PipelineResult
  structure for multi-domain display

  - **Updated references** — added router.ts and reflection pipeline as key reference files

  - **Preserved** all existing knowledge about content principles, injection, trimming, and remaining TODOs
---
date: 2026-03-05T13:38:40.241Z
domain: expert-extension
session: /Users/cgn/.pi/agent/sessions/--Users-cgn-git-0xcgn-agents--/2026-03-05T13-27-24-780Z_ab08f551-a1fe-4814-91e0-3744ac608b47.jsonl
model: current session model
summary: >-
  - **Trimmed architecture section** — removed file-by-file listing that violates the 10-second rule (agent can `ls` and
  `grep`). The code structure is obvious from the files themselves.

  - **Trimmed design_decisions** — removed "Cheap model for reflection" (obvious from settings), "Active trimming on
  every reflection" (detail of the prompt, not architecture), and "Expertise bloat signals codebase problems" (already
  in the reflection prompt constant, not an architecture decision).

  - **Consolidated router conversation format details** — removed the blow-by-blow of format_conversation_for_router
  (user msgs full, assistant summarized, etc.) since this is visible from reading helpers.ts. Kept only the high-level
  design decision.

  - **Removed duplicate gotchas** — "If the cheap model doesn't follow XML format, parse fails gracefully" is just
  describing what the code does. Kept the fragility warning instead.

  - **Removed PipelineResult structure gotcha** — implementation detail visible from types.ts.

  - **Net reduction**: ~95 lines → ~60 lines, well within the 30-60 line target.
---
date: 2026-03-05T13:47:16.140Z
domain: expert-extension
session: /Users/cgn/.pi/agent/sessions/--Users-cgn-git-0xcgn-agents--/2026-03-05T13-27-24-780Z_ab08f551-a1fe-4814-91e0-3744ac608b47.jsonl
model: current session model
summary: >-
  - **Added "In-process LLM calls" design decision** — documents WHY subprocess spawning was replaced (API key
  inheritance failure, lock file conflicts) and the reference pattern (answer extension's extraction.ts). This is the
  key architectural insight from the conversation.

  - **Added "Model selected once, passed down"** — captures the refactoring that removed redundant model selection from
  the router.

  - **Removed overview architecture section** — the file-by-file flow description violated the 10-second rule; the code
  structure is self-evident.

  - **Updated references** — added llm.ts as key reference with its lineage from the answer extension pattern.

  - **Removed "Domain-filtered conversation" as separate design decision** — merged into the two-stage reflection
  description since it's an implementation detail of that design.

  - **Removed "Selective injection over global context"** — obvious from reading the hook code.

  - **Net result**: ~55 lines, focused on non-obvious WHY decisions.
---
date: 2026-03-05T14:06:59.633Z
domain: expert-extension
session: /Users/cgn/.pi/agent/sessions/--Users-cgn-git-0xcgn-agents--/2026-03-05T14-03-21-758Z_39f65d69-bb75-4fc6-aa4f-da699fd3cdcf.jsonl
model: (default)
summary: >-
  - **Added new design decision** about trimming philosophy change — this is the key insight from the conversation.
  Documents WHY thresholds were raised and the pushback mechanism was added (agents were silently dropping valuable
  info).

  - **Removed reference entries** for router.ts and reflection.ts — these are discoverable by reading the code, unlike
  the llm.ts pattern which has non-obvious ancestry from the answer extension.

  - **Simplified gotchas** — merged domain param note into the two-stage reflection design decision where it belongs,
  added note about CONTENT_PRINCIPLES propagation scope since that's a non-obvious coupling.
