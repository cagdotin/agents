# Specs

Implementation specs for planned or complex work live here.

## Conventions

- Use kebab-case file names with a date prefix when useful (e.g. `2026-03-07-feature-name.md`).
- Prefer generating specs via the `create-spec` skill.
- Cross-link specs from related execution plans in `docs/exec-plans/`.
